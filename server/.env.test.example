DATABASE_URL="postgresql://user:password@localhost:5432/billa_test"

JWT_ACCESS_SECRET="change-me"
JWT_ACCESS_TTL="15m"
JWT_REFRESH_TTL="30d"

UPLOADS_DIR="./uploads-test"

# tests mock the http call, so this service doesn't need to actually be running
REMBG_SERVICE_URL="http://localhost:8000/remove-background"
